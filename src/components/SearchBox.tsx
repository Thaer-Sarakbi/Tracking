import React from 'react';
import { View } from 'react-native';

const SearchBox = () => {
    return (
      <View style = {{ height: 1, width: '100%', backgroundColor: '#BDBDBD', marginVertical: 15 }} />
    )
}

export default SearchBox